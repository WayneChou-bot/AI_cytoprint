name: morpho
channels:
  - conda-forge
dependencies:
  - python=3.11
  - git-lfs          # 取 JUMP-Target(CPJUMP1) profiles 用
  - pip
  - pip:
    - pandas==2.2.2
    - numpy==1.26.4
    - pyarrow==16.1.0
    - scikit-learn==1.5.1
    - umap-learn==0.5.6
    - requests==2.32.3
    - harmonypy==0.0.10
    - s3fs==2024.6.1      # JUMP-MOA(S3 匿名讀取)用
    - polars==1.5.0       # 讀 JUMP parquet 用
    # 影像管線 images/image_pipeline.py
    - scikit-image==0.24.0
    - tifffile==2024.7.24
    - imagecodecs==2024.6.1   # 讀 LZW 壓縮 TIFF
    - Pillow==10.4.0
    - scipy==1.14.0
